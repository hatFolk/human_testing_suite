# tells the user if the number they enter is divisible by 5.


num = int(input ("What is the number?"))

if (  (num%5) == 0) :
    print (num,"is divisible by 5.")
else:
    print (num,"is not divisible by 5.")

# MODS:

# only perform printing outside the if-else stmt.

# how could this be done with just an if-stmt
# (instead of an if-else stmt)?

# let the user also input what number they 
# want to divide by.
