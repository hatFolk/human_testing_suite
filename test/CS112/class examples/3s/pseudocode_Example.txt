# celsius to fahrenheit converter program.
BEGIN
	celsius = get input from user:  "temp?"
	fahrenheit = 32 + (celsius / (5/9))
	print fahrenheit temp.
	IF cond:
		bah
		blah
	ELSE:
		sadf
		asdf
	ENDIF
	
	sum = 0
	FOR each item in the list:
		add it to sum
	print the sum
END

######################################################

# powers program.

BEGIN
	num = int(input("what is the number?"))
	print(num, num**2, num**3)
END

BEGIN
	num = int(input("what is the number?"))
	num2 = num**2
	num3 = num**3
	print(num, num2, num3)
END

BEGIN
	num = int(input("what is the number?"))
	num2 = num*num
	num3 = num*num2
	print(num, num2, num3)
END
















